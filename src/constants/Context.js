import { createContext } from "react";


export const infProfile = createContext(['', () => {}]);

export const percent = createContext([0, () => {}]);

export const TodayHab = createContext([0, () => {}]);